#ifndef __EPOS_NNET_ASSERT__
#define __EPOS_NNET_ASSERT__

#include <assert.h>

#define ASSERT(x) assert(x)
#define REGSTRUCT(x)
#define REGSTRUCTC(x,y)
#define REGMAP(x,y,z)
#define REGSET(x,y)
#define REGVECTOR(x,y)
#define REGSMALLVECTOR(x,y,z)
#define REGVECTORC(x,y,z)


#endif
